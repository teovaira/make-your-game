// Shared constants — Theo & Vasiliki agree on values together (ROADMAP.md §4.1, §5 Phase 1).
// No test needed for this file per ROADMAP.md — pure data, not logic.

export const GRID_COLS = undefined; // 13
export const GRID_ROWS = undefined; // 11
export const TILE_SIZE_PX = undefined; // 40
export const TICK_MS = undefined; // 1000 / 60
export const MAX_DELTA_MS = undefined; // 100
export const BOMB_FUSE_MS = undefined; // 3000 — do not use 2500
export const EXPLOSION_DURATION_MS = undefined; // 500
export const PLAYER_SPEED = undefined; // px/ms
export const ENEMY_SPEED = undefined; // px/ms
export const DEFAULT_MAX_BOMBS = undefined; // 1
export const DEFAULT_BLAST_RADIUS = undefined; // 1
export const DEFAULT_LIVES = undefined; // 3
export const STAGE_TIME_LIMIT_MS = undefined; // ~200000
export const ENEMY_POINTS = undefined; // Record<string, number>
export const KEY_BINDINGS = undefined; // Record<string, Direction | 'bomb' | 'pause'>
