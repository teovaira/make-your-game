## Setup
`npm install -D vitest jsdom eslint @eslint/js prettier`

## Build
None — no build step. Open `index.html` directly, or serve statically (e.g. VS Code Live Server).

## Test
`npx vitest run`

## Run
Open `index.html` in a Chromium-based browser, or serve the project root with any static file server.
